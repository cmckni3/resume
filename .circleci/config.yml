version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Create build directory
          command: mkdir build
      - run:
          name: Print the Current Time
          command: docker build -t
workflows:
  version: 2
  build:
    jobs:
      - build
